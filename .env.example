# KhmerCareer frontend runtime configuration
# Production should point to the deployed backend API base URL.
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Keep demo auth disabled by default. Set to true only for local demos without backend access.
VITE_ENABLE_DEMO_AUTH=false
